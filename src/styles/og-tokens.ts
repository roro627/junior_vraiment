// Satori does not resolve CSS variables. Keep the approved semantic palette here
// and assert parity with tokens.css rather than embedding arbitrary card colors.
export const ogTokens = {
  surface: "#edf4fa",
  ink: "#252a31",
  brand: "#cb3f12",
  onBrand: "#ffffff",
  muted: "#59616c",
  border: "#e5e9ed",
} as const;
